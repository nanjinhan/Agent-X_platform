# 이어서하기 — 작업 인수인계 문서

> **이 문서의 목적**: 누가 이어받든(다음 세션의 Claude Code, 다른 개발자) 여기만 읽으면
> "어디까지 했고, 다음에 뭘 해야 하는지" 알 수 있게 한다.
> **규칙: 깃허브에 푸시할 때마다 이 문서를 먼저 갱신한다.**

## ▶ 이어서 하려면 (다음 세션에 이것만 복붙)

새 Claude Code 세션(또는 새 PC)에서 아래 한 줄을 그대로 붙여넣으면 이어서 작업할 수 있다:

```
https://github.com/nanjinhan/Agent-X_platform.git 클론해서 이어서하기.md 읽고 이어서 작업해줘.
```

그러면 다음 순서로 진행하면 된다:
1. `git clone https://github.com/nanjinhan/Agent-X_platform.git && cd Agent-X_platform`
2. **이 이어서하기.md 읽기** ← 현재 위치·다음 작업·규칙이 여기 다 있음
3. 아래 [4. 개발 환경 셋업](#4-개발-환경-셋업-새로-받은-사람용)대로 환경 구성 (`pnpm install` → `docker compose up` → 마이그레이션 → `.env`)
4. 아래 [3. 다음 작업](#3-다음-작업-t6--해시체인서명-️-비가역--구현-전-설계-리뷰-필수)부터 이어서

> ⚠️ `.env`는 깃허브에 없다(시크릿). `cp .env.example .env`로 로컬 기본값 생성 — 그대로 동작함.

---

**마지막 갱신**: 2026-08-27
**현재 위치**: **T1~T15(M1)·T16·T17 완료** + 프론트(cult-ui) → **다음 작업: T18 (환불 — 청약철회 REG-014, 수신 시그널 차감 REG-016)**

> **Phase 1 완료 + Phase 2 진행 중(T16 subscription·T17 billing ✅).** 자체 에이전트 3개 페이퍼 발행 중.
> 프론트 dev(:4000), core-api(:3000), signal-service(:3001) 기동 상태(이 세션). 실데이터 배치는 시장데이터 백필 후.
> 지금 프론트 숫자는 전부 목 데이터 — T13이 있어야 진짜 랭킹이 채워진다.
> ※ T8(공개 검증 API)은 T6에서 선구현됨. 장중 5분 VWAP은 분봉 데이터 수집 후 실동작(현재 로직만).

> **참고 — 프론트엔드 (apps/web, 로드맵 순서 밖 조기 착수)**: `apps/web`에 **Next.js 15 + Tailwind v4
> + cult-ui** 프론트를 목 데이터로 구성. 4개 화면(랭킹/에이전트/시그널/투명성) 렌더 검증 완료.
> **2026-08-27: cult-ui 실제 이식 완료** — 이전엔 이름만 적히고 안 쓰였던 texture-card·gradient-heading·
> texture-button을 nolly-studio/cult-ui에서 실제로 가져와 4화면에 적용(⚠️ gradient-heading은 Radix Slot →
> `"use client"` 필수). 실행: `pnpm --filter @signals/web dev` → localhost:4000. 실데이터 연동(`src/lib/mock.ts`
> → `/v1` fetch)은 T13 완료 후. **백엔드 크리티컬 패스는 계획대로 계속 진행.**

---

## 1. 프로젝트 개요

한국형 투자 시그널 에이전트 마켓플레이스 **SIGNALS**.
유사투자자문업 모델. 핵심 차별점은 해시체인 기반 조작 불가능한 성과 검증.

| 문서 | 내용 |
|---|---|
| [VISION.md](./VISION.md) | 비전 초안 v0.1 — 사회적 미션(소비자 보호), 확장 층위(교육→ESG→광주), 경계. M3 회고 때 개정 |
| [SIGNALS 요구사항지시서 v1_0.md](./SIGNALS%20요구사항지시서%20v1_0.md) | SRS 원문 (5,047줄). 요구사항 ID(REG/AGT/SIG/PERF/SYS…)의 출처 |
| [PROJECT-STRUCTURE.md](./PROJECT-STRUCTURE.md) | 저장소 구조 설계 — 모듈 경계, 테이블 소유권, 규제 강제 포인트 |
| [docs/TASKS.md](./docs/TASKS.md) | 구현 태스크 T1~T30, 상태 표기 포함 |
| [docs/adr/](./docs/adr/) | 아키텍처 결정 기록 (ADR-0001: NestJS 채택) |

## 2. 완료된 것

### 스캐폴드 (2026-07-21)
- pnpm 모노레포: `apps/core-api`(NestJS, 10개 모듈), `apps/signal-service`, `apps/notification-service`, 프론트 3종 스텁, `batch/`(Python), `packages/{domain,contracts,database,ui}`
- `@signals/domain`: SRS 6.3 상태머신·열거형 구현 완료
- `@signals/contracts`: 에러 코드(SYS-024)·도메인 이벤트(SYS-004) 타입 완료
- TS 전체 빌드 통과, core-api 부팅 확인됨

### T1 — 로컬 인프라 (2026-07-21 완료)
- `docker-compose.yml`: Postgres 15(**호스트 포트 5433** — 5432는 이 PC의 다른 프로젝트가 점유), Redis 7
- 마이그레이션 0001(SRS 16.1 스키마 26개 테이블)·0002(초기 파티션) 적용 검증
- **SRS 정오 발견**: 16.1의 `notification_deliveries`·`audit_logs`는 단독 PK로는 파티션 생성 불가 → 복합 PK로 정정 (마이그레이션 파일에 주석 있음)
- 스모크 테스트: 시그널 불변 트리거(`Signal content is immutable`) 작동 확인

### T2 — 국내 시장데이터 수집 (2026-07-21 완료)
- **소스 전환 결정**: KRX data.krx.co.kr 직접 호출이 이 네트워크에서 400 "LOGOUT" 차단
  → **네이버 금융 시세 API(`naver_client.py`) + KIND 상장법인목록(`kind_client.py`)** 조합으로 구현.
  `krx_client.py`는 차단 기록과 함께 보존 (정식 데이터 계약 시 재구현 지점).
- 실행: `cd batch && .venv/Scripts/python -m signals_batch.ingestion.kr.backfill --days 45`
- 결과: 2,635종목 × 개장일 30일 = **75,125행 적재**, 미거래일(OHLC 없음+거래량 0) 2,769행 거부, 플래그 0
- 품질검증(SYS-009) 6종 구현: `ingestion/quality/validators.py` + 단위테스트 14건 (`pytest` 통과)
- 거래 캘린더: market='KR' 공통, "데이터 존재 = 개장일" 방식 (정식 휴장일 소스는 후속)
- **후속 보강 필요** (T9 슬리피지·REG-013 전까지): 시가총액, 거래대금(value), ETF 마스터, 수정주가 계수

### T3 — 미국·환율·벤치마크 수집 (2026-07-21 완료)
- **무료 구현**: Polygon(키 필요) 대신 **야후 파이낸스 차트 API**(`yahoo_client.py`, 키 불필요).
  개별주·지수·환율이 같은 엔드포인트. 실행: `python -m signals_batch.ingestion.us.backfill --days 45`
- 적재: 미국 34종목(NASDAQ 16 + NYSE 18) 986행, USDKRW 31행, 벤치마크 SPX_TR/KOSPI200_TR 각 29, US 캘린더 29개장일
- **주의할 한계**:
  - `KOSPI200_TR`은 실제로 **가격지수(`^KS200`) 근사** — 무료 TR 소스 부재. 배당수익률(~2%/년)만큼 벤치마크가 과소 → 알파(PERF-015)가 그만큼 과대 계상됨. **정식 데이터 계약 시 실제 TR로 교체 필수.**
  - 미국 유니버스는 전체가 아니라 유동성 상위 34종목 시드(`us_universe.py`). 자체 에이전트 시연엔 충분하나 확장 필요.
  - 거래대금·시가총액·수정주가 계수 여전히 없음(T2와 동일 후속 항목).

### T4 — identity 모듈 (2026-07-23 완료)
- **여기서부터 NestJS 백엔드 실구현 시작.** DB 접근은 `pg` 직접 + 모듈별 리포지토리(ORM 없음).
- 공통 인프라 신설: `apps/core-api/src/common/` — env(zod 검증), DB Pool, Redis, AES-256-GCM Cipher(SEC-009),
  RFC7807 예외필터(SYS-024), zod 본문 파이프, JWT 가드(SEC-005 deny-by-default)
- identity 모듈: api / application / infra / domain 레이어. 공개 인터페이스 = `IdentityService`(SYS-003)
- **API 8종**(SRS 17.2, 전부 E2E 검증): register, login, social/:provider, refresh, logout, withdraw,
  verify/phone/request, verify/phone/confirm
- JWT: Access 30분(본문) + Refresh 30일(HttpOnly 쿠키), **회전 + 재사용 감지 → 전 세션 무효화**(SYS-025)
- 만 19세 차단(REG-020), Argon2id(SEC-002), 이름 암호화·phone/ci 해시(SEC-009), 탈퇴 익명화(SYS-022)
- **주의/한계**:
  - **본인인증은 mock PASS**(`infra/pass.provider.ts`) — 코드 발급이 서버 로그에 찍힘. name/birthDate/ci는
    confirm 요청 바디로 받음(실 PASS 콜백 대체). Phase 0에서 실 PASS(NICE/KCB) 어댑터로 교체.
  - **소셜 로그인도 mock** — OAuth 토큰 검증 대신 확인된 email을 바디로 받음. 미가입자는 본인인증 후 가입 유도(403).
  - **SRS 정오**: users에 `password_hash` 컬럼 누락(SEC-002 요구) → **마이그레이션 0003** 추가.
  - 탈퇴 시 email이 NOT NULL이라 NULL 대신 `withdrawn+{id}@deleted.invalid`로 익명화.
- 로컬 검증 방법: `docker compose up -d` → 0001~0003 적용 → `node apps/core-api/dist/main.js`(repo 루트에서, .env 자동 로드)

### T5 — provider·agent 모듈 (2026-07-23 완료)
- provider: 신청(`POST /v1/provider/apply`, bio 200자+·금칙어 검사) → 관리자 승인(`POST /v1/admin/providers/:id/approve`)
  → **얼리버드 판정 자동**(선착순 100, 수수료 0.10 평생, SUB-001) + `IdentityService.promoteToProvider`로 역할 승격.
  공개 인터페이스 `ProviderService`(agent 모듈이 사용).
- agent: CRUD + **AGT-003 상태머신** — 전이는 `@signals/domain`의 `canTransition` + DB `WHERE status=기대값`
  낙관적 검증(경쟁 조건 방지). 자동심사 서브셋(AGT-018): 금칙어(`findProhibitedTerm`), 이름 UNIQUE,
  티어 잠금(신규는 T0~T2만, SUB-003), 공급자당 에이전트 수 제한. `genesis_hash`는 생성 시 발급(SIG-005).
- RBAC: `RolesGuard` + `@Roles(ADMIN)` — 관리자 전이 API(review/activate/approve)는 ADMIN 전용.
- E2E 검증 완료: 정상 체인 DRAFT→PENDING→VERIFYING→ACTIVE + 불법 전이 4종 409 + 비관리자 403 + 금칙어 422.
- **주의/발견**:
  - domain에 `HoldingPeriod` 5구간 신설 (SRS AGT-005가 값 미정의 → 설계 결정, enums.ts 주석 참조)
  - **U+FFFD(인코딩 깨짐) 입력 거부** 검증 추가 — Windows curl이 한글 인자를 CP949로 보내 금칙어가
    우회되는 사고를 E2E에서 발견. **한글 바디 테스트는 반드시 `--data-binary @file`(UTF-8 파일)로 할 것.**
  - pg 파라미터를 SQL에서 두 타입으로 쓰면 42P08 → `$n::varchar` 명시 캐스팅 (agent.repository.transition 참조)
  - AGT-020/023 검증 기간 정량 판정(30일·15건·MDD)은 성과 엔진(T9~T12) 후 자동화 — 지금은 관리자 수동 activate

### T6 — 해시체인·서명 (2026-07-24 완료) ⚠️ 규격 영구 고정됨
- **규격의 단일 출처: [docs/adr/0002-hash-chain-and-signature.md](./docs/adr/0002-hash-chain-and-signature.md)**
  — 정규 직렬화(이스케이프 규칙 포함), ECDSA P-256/SHA-256, 키 관리·회전. **첫 실발행 이후 변경 금지(TR-4).**
- 구현: `apps/signal-service/src/` — `hashchain/`(canonical·hasher·signer·key-manager),
  `publish/`(발행 트랜잭션+멱등성), `verify/`(공개 검증 3종: 시그널/체인/공개키 — SYS-027/028)
- **검수 1.1~1.5 전부 통과**: 불변 트리거 차단, 체인 연결(genesis→#1→#2→#3), 공개키 서명 검증,
  트리거 우회 변조 → `CONTENT_TAMPERED` 즉시 탐지, 멱등성(동일 키 재발행 시 기존 반환)
- **SRS 대비 보완 3건** (전부 ADR·마이그레이션에 기록):
  1. 해시 입력 이스케이프 규칙 신설 (rationale의 `|`로 필드 경계 위조 가능하던 구멍)
  2. `signing_key_id` 컬럼 + `signing_keys` 공개키 레지스트리 (마이그레이션 0004) — 키 회전 후 과거 검증 보장
  3. **해시 템플릿 확장**: `suggested_weight`·`max_holding_days`·`valid_until` 추가 + 트리거 보호(0005)
     — 봉인 밖에 두면 체인 VALID인 채로 성과 조작 가능하던 구멍 (실증 후 수정)
- 키: 개발용 `.keys/` 자동 생성(gitignore), 운영은 KMS로 교체 지점 = `KeyManager.sign()`

### T10 — 자동 청산 판정 (2026-07-24 완료)
- `performance/settlement.py`(순수) + `run_settlement.py`(배치). OPEN 포지션 일봉 스캔 →
  목표가/손절 장중 터치, **갭 처리(SIG-020: 시가가 이미 넘으면 청산가=시가)**, max_holding TIME_LIMIT 종가,
  에이전트 ARCHIVED 강제청산(AGT-003). 동일 봉 양방 터치는 순서 불명 → **보수적 손절 우선**(성과 과대 방지).
- 배치는 `status='OPEN' AND exit_signal_id IS NULL`만 대상(수동 EXIT는 T7 페어링 소관).
- 단위테스트 14건(검수 2.3~2.6 포함) 통과. 실행: `python -m signals_batch.performance.run_settlement`
- **T12가 알아야 할 확정 규약 2건** (골든 수기계산도 이대로 따라야 0.01%p 통과):
  1. **보유일 규약**: 진입일 당일 제외, **다음 거래일부터** 판정(진입일=0일차). 당일 익절을 다음날로
     미뤄 항상 보수적(과대평가 없음, PERF-001). NEXT_OPEN·INTRADAY 진입 동일 처리. (settlement.py docstring 참조)
  2. **동일 봉 목표·손절 동시 터치** → 일봉으론 순서 불명 → **보수적 손절 우선**.
- **후속(T2/T3와 동일 대기)**: 상장폐지 FORCED는 상폐 데이터 소스 확보 후 연결(현재 상폐 플래그 없음).
- **경계(T7 소관) 발견**: 수동 EXIT는 `exit_signal_id`만 세팅되고 `status`=OPEN·`exit_price`=NULL로 남음
  (pairing.service.ts closePosition). DISCRETIONARY 청산가(PERF-004: 익일시가/VWAP) 산출·CLOSED 전환이 미완.

### T11 — 수익률·시뮬레이션 (2026-08-03 완료)
- `performance/returns.py`(순수) + `run_returns.py`(배치). T10이 확정한 진입가·청산가에 **현실 비용**을
  입혀 net_return을 만든다(성과 과대평가 방지, 부록A #6). 배치는 `status='CLOSED' AND net_return IS NULL`을
  훑어 positions의 비용·수익률·환율 컬럼을 채운다. 실행: `python -m signals_batch.performance.run_returns`
- **거래비용(PERF-005)**: 국내 매수 0.015%/매도 0.215%(거래세 0.20% 포함), 미국 매수 0.07%/매도 0.0708%(SEC fee 포함).
- **슬리피지(PERF-006)**: `base_slippage(시총구간) × liquidity_multiplier(참여율 클램프 1.0~2.0)`.
  - ⚠️ **시가총액 데이터 미수집(T2/T3 후속)** → 시총 NULL이면 **소형주(최대 슬리피지)로 보수 처리**(성과 부풀림 방지).
    instruments.market_cap 채워지면 자동으로 구간 반영됨.
  - ⚠️ **설계결정**: SRS가 미국 중형/소형 경계 미명시 → 통상 구분 **$2B** 채택(대형 ≥$10B, 중형 $2B~$10B).
  - 참여율(구독자 주문총액/일평균거래대금)은 배치가 산출·주입. 현재 페이퍼 단계(구독자 0) → 승수 1.0.
- **net_return(PERF-009)**: `(청산가×(1-매도비용-슬리피지)) / (진입가×(1+매수비용+슬리피지)) - 1`. **LONG/SHORT 대칭**
  (SHORT는 진입=매도·청산=매수). gross_return은 비용 미반영 참고치.
- **통화(PERF-007)**: 미국 포지션은 **달러 net_return이 주 기준**(랭킹도 달러), 원화환산 net_return_krw 병기.
  환율은 진입일/청산일 각각 fx_rates USDKRW 매매기준율(주말·휴일은 이전 최근일 fallback). KR은 net_return_krw=NULL.
- **가상 포트폴리오 시뮬레이션(PERF-010B/011/012)**: `simulate_portfolio` 순수 함수 — 초기자본 100, suggested_weight
  (없으면 1/max_positions) 사이징, **자금 부족·슬롯 초과 시 스킵 기록**(검수 2.13), 레버리지·리밸런싱 없음.
  - ⚠️ **설계결정**: 보유 중 포지션은 일간 마킹 데이터가 없어(T12 스냅샷 소관) **취득원가 유지**, 실현손익만 복리.
    같은 날짜는 청산→진입 순 처리.
- 단위테스트 **18건**(검수 2.7 거래비용·2.8 슬리피지·2.9 달러원화·2.13 스킵) 통과. 파일: `tests/test_returns.py`.
  **전체 배치 스위트 60건 통과, `ruff check` 클린**(Python 3.12.10을 winget으로 설치 후 실행 확인 완료).
- **✅ 라이브 DB E2E 완료 (2026-08-03)**: docker DB에 3케이스 시드(KR 대형·KR 소형(시총 NULL)·US) 후
  `run_settlement`(T10)→`run_returns`(T11) 순차 실행 → positions 비용·수익률·환율 컬럼 채워짐 확인.
  - KR 대형(시총 5e14): 매도비용 0.215%(거래세)·슬리피지 0.05%·net 10.8831% (검수 2.7·2.8)
  - KR 소형(시총 NULL): **슬리피지 0.20%(보수적 소형 기본값)**·net 10.5506% (검수 2.8 시총 차등)
  - US(AAPL): 매도비용 0.0708%(SEC fee)·net 9.6696%(달러) + **net_krw 11.3568%(환율 1300→1320)** (검수 2.7·2.9)
  - 저장값이 순수함수 손계산과 일치. 시드 SQL은 세션 scratchpad(`seed_t11.sql`) — 재현 시 재사용.
- **T12 경계**: 시뮬레이션 결과(equity curve·skip_rate)의 영속화는 T12(agent_daily_snapshots/agent_metrics) 소관.
  T11은 포지션 단위 net_return까지만 DB에 채운다. 시뮬레이션 엔진은 재사용 가능하게 순수 함수로 분리해 둠.
- **net_return 통화 규약(T12/T13 주의)**: net_return 컬럼은 **각 시장의 네이티브 통화 수익률**(US=USD, KR=KRW).
  수익률은 무단위 비율이므로 랭킹에서 그대로 비교 가능. 원화 체감치가 필요하면 net_return_krw(US만).

### T12 — 지표 산출 + 골든 테스트 (2026-08-27 완료)
- `performance/metrics.py`(순수 지표 전종) + `snapshots.py`(일별곡선) + `run_metrics.py`(배치).
  실행: `python -m signals_batch.performance.run_metrics`
- **지표(PERF-013~018)**: 수익성(누적·CAGR·평균/중앙/최고/최저), 위험(변동성·MDD+기간·회복·하방변동성·VaR95),
  위험조정(샤프·소르티노·칼마·정보비율·알파·베타), 실행(승률·Wilson 하한·손익비·평균이익/손실·기대값·평균보유일·
  최대연속손실), 규율(목표가 도달률·VOID율). 전부 순수함수.
- **표본 게이팅(PERF-016)**: 샤프/소르티노 30건·90일, 승률 20건·30일, MDD 20건·60일, 손익비 20건·30일.
  미달 시 None("산출 중") → 랭킹 제외(T13).
- **일별 스냅샷**: T11 `simulate_portfolio`에 `entered_positions`(배분·진입/청산일) 출력을 추가 → snapshots가
  거래일마다 cash+open원가로 투영. ⚠️ **T11 원가모델 그대로**(보유분 원가 유지, 실현손익 계단) → daily_return은
  청산일에만 튄다. **진짜 일간 MTM(일봉 평가)은 후속**.
- **확정 규약 3건**:
  1. **정밀도**: 포지션 net_return은 Decimal(T11), 지표는 float(sqrt·백분위 있어 quantstats 호환, 오차 1e-4).
  2. **CAGR 30일 미만 가드**: 짧은 기간 연율화는 폭발(NUMERIC 오버플로) → None. (실데이터 T15는 30일+라 무관)
  3. **Rf(무위험) = 3.2% 파라미터** 기본값(3년 국고채 근사) — ECOS 실측 연동은 후속.
- **골든 테스트 27건**(`test_metrics.py`) + snapshots 4건 통과. Wilson(50/100→0.4038, 10/10→0.7225)·MDD(기간·회복)·
  샤프·소르티노·VaR 등 **수기 대조 1e-4**. ※ quantstats 교차검증(검수 3.1)은 무거운 의존성이라 미도입 — 수기 골든으로 대체.
- **✅ 실데이터 E2E**: 시드 3 CLOSED로 `run_metrics` → agent_metrics·agent_daily_snapshots 저장 확인.
  누적 10.7169%(포트폴리오 방식B, 1건 자금부족 스킵)·평균 10.3678%(방식A 3건)·스킵률 33.3%·목표가도달률 1.0.
  CAGR/샤프/승률/MDD는 **표본 게이팅으로 NULL**(1일·3건 < 임계 → 정상 동작 확인).
- **T13이 알아야 할 것**: 랭킹 기본 지표는 skip_rate 반영한 포트폴리오 수익률·**Wilson 하한**(단순 승률 아님, PERF-018).
  SIGNALS Score 위험조정 = 0.6×소르티노 + 0.4×칼마 (SRS 2025). 최소표본 20건 미만은 랭킹 제외(PERF-016).

### T13 — 랭킹 (2026-08-27 완료)
- `ranking/scoring.py`(순수) + `ranking/run_ranking.py`(배치). 실행: `python -m signals_batch.ranking.run_ranking`
- **SIGNALS Score 6요소(RANK-004)**: 위험조정(30%, 0.6×소르티노+0.4×칼마)·초과수익(20%, alpha)·일관성(15%,
  1−CV(월수익)+0.5×양수월비율)·유지율(15%)·규율(10%, 1−0.4·VOID−0.4·미이행−0.2·빈도이탈)·표본신뢰(10%,
  log(1+완결)/log(101)). **리그·부문 그룹 내 Z정규화 후 가중합**.
- **리그(RANK-002)**: 최근 90일 시그널 90%+ 국내→KR, 90%+ 미국→US, 아니면 MIXED. **부문(RANK-003)**:
  CONSERVATIVE/MODERATE_CONS→STABLE, NEUTRAL→NEUTRAL, AGGRESSIVE/VERY_AGGRESSIVE→AGGRESSIVE.
- **노출 자격(RANK-005 + 저빈도 예외 RANK-006)**: ACTIVE·완결≥20(VERY_LOW 8/LOW 12)·운영≥60일(180/120)·
  청산미이행≤30%·VOID≤10%·최근30일 시그널. 미달은 정규화·랭킹에서 제외하고 사유와 함께 agent_rankings에 기록.
- **확정 규약 2건(문서화)**:
  1. **Z-score는 자격자만**으로 산출. 그룹 표본 1이거나 표준편차 0이면 z=0(중립).
  2. **미수집 구성요소(유지율·월수익률 등)는 z=0(중립)** 처리 — 데이터 없어도 랭킹 붕괴 안 함. 확보 시 자동 반영.
- 골든 **19건**(`test_ranking.py`) 통과: 6요소 공식·Z정규화(None중립)·자격 게이트·저빈도 예외·리그 90%룰·그룹 점수.
- **✅ 실데이터 E2E**: 시드 에이전트 → MIXED/NEUTRAL 분류, **미자격 "완결 포지션 3 < 20"**(RANK-005 게이트),
  sample_confidence 0.3004(=log4/log101 손계산 일치)·discipline 1.0, score_components(raw+z) JSONB 저장 확인.
- **미완/후속**: (1) freq_deviation·unclosed_rate는 agent_metrics에서 아직 NULL(T12 미산출) → T14/이상탐지에서 채우면
  규율·자격에 자동 반영. (2) **랭킹 조회 API**는 T23(구독자 웹)에서 노출 — 지금은 배치 산출·저장까지. (3) 리그별
  벤치마크 CAGR을 alpha에 직접 쓰는데, 현재 alpha는 T12가 벤치마크 없이 낸 값(KOSPI200_TR TR 근사 한계, T3 참조).

### T14 — 감사·무결성 배치 (2026-08-27 완료)
- `verification/chain_verify.py`(순수) + `run_verification.py`(배치). 실행: `python -m signals_batch.verification.run_verification`
- **일일 전체 체인 재검증(SEC-018)**: 에이전트별 해시체인을 재계산·재검증 — (1) content_hash 재계산 일치, (2) prev_hash
  체인 연결(첫 시그널=genesis), (3) **ECDSA P-256 서명 검증**(cryptography 신규 의존성 추가). verify.service.ts verifyChain +
  서명까지.
- **⚠️ 바이트 일치 확정**: chain_verify의 정규직렬화는 signal-service `canonical.ts`와 **바이트 단위로 동일해야** 정상
  시그널을 위조로 오판하지 않는다(TR-4). Node(tsx)로 TS 원본을 실행해 **동일 입력→동일 SHA-256**(`3b89034065cd…`) 교차
  대조로 검증 완료. published_at ms 포맷·가격 4자리·이스케이프(\\→\\\\, |→\\|)·빈 valid_until 전부 일치.
- **위조 대응(완료 기준)**: 체인 BROKEN 발견 시 → anomaly_flags **CRITICAL** 기록 + 에이전트 **SUSPENDED**(자동 격리) +
  audit_logs **append-only** 기록(action=CHAIN_TAMPER_DETECTED). 정상 체인은 멱등(아무것도 안 바꿈).
- 골든 **12건**(`test_chain_verify.py`): 정규직렬화 exact·서명 round-trip·CONTENT_TAMPERED/PREV_HASH_MISMATCH/
  SIGNATURE_INVALID/NO_PUBLIC_KEY 탐지.
- **✅ 실데이터 E2E**: 유효 서명 체인(agent 44444, 실제 P-256 서명) → **VALID**, 가짜 해시 시드(agent 33333) →
  **BROKEN → SUSPENDED + CRITICAL anomaly + audit_logs** 기록 확인. `{agents:2, valid:1, broken:1, suspended:1}`.
- **후속**: 전면 audit 인터셉터(모든 민감 작업 자동 로깅, SEC-023 전체)는 core-api NestJS 미들웨어로 별도 —
  지금은 무결성 배치의 탐지 기록만 audit_logs에 남긴다.
- **주의**: 이 E2E에서 **기존 시드 에이전트(33333)가 SUSPENDED로 격리됨**(가짜 해시라 정상 동작). 이후 T11~T13 재실행 시
  이 에이전트는 제외됨. 유효 체인 에이전트(44444)는 ACTIVE.

### T15 — 내부 운영 도구 → 🎉 M1 달성 (2026-08-27 완료)
- `ops/platform_agents.py`. 실행: `python -m signals_batch.ops.platform_agents` (전제: signal-service :3001 기동)
- **자체 에이전트 3개(AGT-001)**: 든든(안정추구·MODERATE_CONS·국내·저PBR가치), 파도(중립·NEUTRAL·혼합·섹터로테이션),
  질주(공격·AGGRESSIVE·미국·모멘텀). 3리그·3부문 스펙트럼 커버. **VERIFYING 상태 = 페이퍼(AGT-002)**.
- **실제 발행 경로**: 발행 엔드포인트(`publish.controller`)가 **ADMIN 토큰을 자체 에이전트 내부 도구로 명시 지원**
  (소유권 검사 생략). 공유 `JWT_ACCESS_SECRET`으로 ADMIN HS256 토큰을 Python에서 발급 → signal-service HTTP로
  실제 발행. OTP 가입 플로우 불필요(플랫폼 내부 도구).
- **✅ M1 E2E**: 3 에이전트 실제 발행 → is_paper=true 시그널 + PENDING 포지션 생성 → **T14 검증 배치로 3개 체인
  전부 VALID**(content_hash+체인+ECDSA 서명). 이는 signal-service(TS) 발행과 Python 검증기의 **바이트 일치를
  실데이터로 재증명**한 것. 단위 4건 통과(JWT 발급·스펙 커버리지).
- **확정 사실**: agents 테이블엔 `is_platform_owned` 없음 → **provider.is_platform_owned로 판정**(JOIN).
  signal-service 부팅 시 KeyManager가 `signals-dev-01` 키를 생성·signing_keys에 등록.
- **후속(30일 페이퍼 트레이딩)**: 지금은 발행 당일이라 성과 배치(T9~T13)는 시장데이터/시간 경과가 필요.
  매일 T2/T3 시장데이터 백필 → confirm_reference_prices → settlement → returns → metrics → ranking 순으로
  돌리면 30일 뒤 실성과·랭킹이 채워진다. 이게 M2 크리티컬 패스.

### T16 — subscription 모듈 (2026-08-27 완료)
- core-api `modules/subscription` (api/application/infra) — agent 모듈과 동일 패턴(서비스=도메인전이+낙관적검증).
- **체험 SUB-005**: 7일 무료 체험 플랫폼 강제. **어뷰징 방지 SUB-016**: `trial_history`(PK user+agent)로 유저·에이전트당
  1회만. **가격 락인**: 구독/체험 시점 `agents.price_krw`를 subscriptions에 고정(이후 인상돼도 유지).
- **상태머신(6.3.3)**: 도메인 `SUBSCRIPTION_TRANSITIONS`(TRIAL→ACTIVE/TRIAL_EXPIRED, ACTIVE→PAST_DUE/CANCELLING,
  CANCELLING→ACTIVE(재개)/EXPIRED) + DB `WHERE status=기대값` 낙관적 전이.
- **API 5종**(JwtAuthGuard): `POST /v1/subscriptions/trial`·`POST /v1/subscriptions`(체험→유료 전환 or 즉시구독)·
  `GET /v1/subscriptions`·`POST /:id/cancel`·`POST /:id/resume`.
- **✅ 실데이터 E2E**: 체험(TRIAL·락인 29900)→재체험 **409 TRIAL_ALREADY_USED**→유료전환 ACTIVE→해지 CANCELLING→
  재개 ACTIVE, trial_history 1행 확인. core-api 빌드 클린.
- **후속(T17 billing)**: 실결제 없음 — 상태·기간·가격락인까지만. billing_key·정기결제·PAST_DUE 전이·TRIAL_EXPIRED
  만료(체험 종료 배치)·웹훅은 T17. `next_billing_at`만 세팅해 둠.

### T17 — billing (PG 연동) (2026-08-27 완료)
- core-api `modules/billing` (api/application/infra). 의존: subscription 결제필드.
- **Mock PG 어댑터**(`infra/mock-pg.provider.ts`): 빌링키 발급·정기결제·웹훅 HMAC 서명. **실 PG(토스/포트원)
  교체 지점**(T4 mock PASS 전략). 결정적: 빌링키에 'FAIL' 포함 시 결제 실패(테스트용). 카드정보 미저장(빌링키만).
- **정기결제(SUB-017)**: `chargeDue()` — next_billing_at 도래분 일괄 청구. 성공 시 payments PAID + 기간연장 +
  다음 결제일 + failed_attempts=0. 매일 배치가 호출(지금은 `admin/billing/charge-due` 수동 트리거).
- **실패 재시도(SUB-018)**: D+0 실패 → PAST_DUE(유예, 시그널 유지) → D+1/D+2/D+3 재시도 → **D+3 실패 시 EXPIRED**
  (failed_attempts > 3). 각 시도 payments FAILED 기록.
- **웹훅 서명 검증**: `POST /v1/billing/webhook` 무인증 — HMAC-SHA256(payload, PG_WEBHOOK_SECRET) 검증. 위조 → 401.
  ※ payload는 서명 대상 원문 문자열 필드로 받음(raw-body 파싱 회피). env에 `PG_WEBHOOK_SECRET` 추가(기본값 있음).
- **API 3종**: `billing/method`(빌링키 등록, 인증)·`billing/webhook`(무인증·서명)·`admin/billing/charge-due`(ADMIN).
- **✅ 실데이터 E2E**: 정상 PAID(기간연장)·실패 PAST_DUE×3→EXPIRED(payments 5행: PAID1 + FAILED4)·웹훅 유효 201/위조 401.
  core-api 빌드 클린. ※ pg 42P08(타입 추론) 회피 위해 markPaymentByTid에 `$2::varchar` 캐스팅(T5 교훈 재확인).
- **후속**: 실 결제일 규칙(SUB-017 매월 동일 일자·말일 조정)은 30일 근사 중. 결제 배치의 크론 스케줄링·REG-017 사전
  고지(7일 전)는 T19 notification과 함께. TRIAL_EXPIRED 만료(체험 종료 배치)도 별도 배치 필요.

## 3. 다음 작업: T18 — 환불

**Phase 2 진행 중** (T16 subscription·T17 billing ✅ → T18 환불).

- **T18**: 청약철회(REG-014, 시그널 미수신+7일 내 전액), 수신 시그널 차감 공식(REG-016), 케이스별 자동/수동(SUB-022~023).
  검수 6.7~6.9. T17이 남긴 seam: payments(REFUNDED/PARTIAL_REFUND status), refunds 테이블, subscriptions.signals_received.
- 이후 Phase 2: T19 notification → T20 moderation → T21 룰엔진 → T22 공급자콘솔 → T23 구독자웹(프론트 실데이터 연동) →
  T24 규제검수(M3).

크리티컬 패스 메모: Phase 1(T1~T15)·M1 완료. Phase 2 진행(T16·T17 ✅). 다음 관문 M3(클로즈베타, T24).

## 4. 개발 환경 셋업 (새로 받은 사람용)

```bash
# 요구: Node 20+, pnpm 9, Docker, Python 3.12+
pnpm install
pnpm build                        # TS 전체 빌드 (의존 순서 자동)

cp .env.example .env              # 로컬 기본값 그대로 동작
docker compose up -d --wait       # Postgres(5433) + Redis(6379)

# 마이그레이션 (아직 러너 미도입, psql 직접 실행 — 번호순 전부)
docker exec signals-postgres psql -U signals -d signals -f /migrations/0001_init.sql
docker exec signals-postgres psql -U signals -d signals -f /migrations/0002_partitions.sql
docker exec signals-postgres psql -U signals -d signals -f /migrations/0003_user_password.sql

# core-api 실행 (repo 루트에서 — .env 자동 로드)
node apps/core-api/dist/main.js   # http://localhost:3000/v1

# Python 배치
cd batch
python -m venv .venv && .venv/Scripts/activate    # Windows
pip install -e ".[dev]"
python -m pytest -q                               # 배치 스위트 전체
```

> ⚠️ **Windows Python 주의**(이 개발 PC 기록): `python`이 Microsoft Store 스텁을 가리켜 안 될 수 있다.
> Python 3.12.10을 winget으로 설치했고 실제 경로는 `%LOCALAPPDATA%\Programs\Python\Python312\python.exe`.
> 스텁이 가리면 이 전체 경로로 직접 호출하거나 venv(`.venv/Scripts/python.exe`)를 쓸 것.

## 5. 절대 어기면 안 되는 것 (요약)

1. **REG-001**: `signals` 테이블에 구독자 참조 금지. signal-service는 subscription 코드를 import하지 않는다. 시그널 생성에 개인화 진입 금지.
2. **SIG-007**: 발행된 시그널 내용 수정 절대 불가 (DB 트리거로도 막혀 있음). 오타도 정정 시그널 재발행.
3. **T6(해시체인)은 비가역**: 해시 입력 필드·서명 방식은 확정 후 변경 시 과거 체인 재구성 불가. 구현 전 설계 리뷰 필수.
4. **SYS-003**: 타 모듈 테이블 직접 SQL 금지. 모듈 공개 서비스 레이어로만.
5. 성과 수치 응답에는 `disclaimer` 필드 필수 (SYS-026), 표기는 기간·표본수·면책 병기 (REG-005).

## 6. 결정 이력 (요약)

- 백엔드 **NestJS 확정** (ADR-0001) — 팀 언어 경계, 모듈 강제, 타입 공유
- 배치는 **Python** (SYS-006 명시)
- turbo 미도입 (pnpm -r로 충분, 앱 늘면 도입)
- 마이그레이션 러너 미도입 (P1 중 node-pg-migrate 또는 dbmate 도입 예정)
